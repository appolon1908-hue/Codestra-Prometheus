# Backup, restore, and rollback

Before a runtime change, capture the current image digest, signed configuration
artifact and checksum, TSDB snapshot or protected volume snapshot, rule state,
target status, and previous approved bundle. Rehearse rollback against an isolated
or canary instance and prove query and alert evaluation health.

This document does not execute a backup, deployment, restore, or rollback.
